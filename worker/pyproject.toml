[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "investments-portfolio-manager-worker"
version = "0.1.0"
dependencies = [
  "playwright>=1.52.0",
]

[tool.setuptools.packages.find]
where = ["."]
